# Student Mini Project (CLI)

This is a tiny project used in Module 4 (Spring Foundations) to *understand* the need for DI/IoC.

## Run (after filling TODOs)

```bash
mvn -q clean package
java -jar target/student-mini-project-1.0-SNAPSHOT.jar
```

